{
  "name": "@hakimi/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "migrate": "tsx src/migrate.ts",
    "schema:verify": "tsx src/schema-verify.ts",
    "docs:validate": "npx redocly lint openapi.yaml",
    "access:provision": "tsx src/access/provision.ts",
    "access:provision:local-demo": "tsx src/access/local-demo-provision.ts",
    "test:integration:db": "vitest run --config vitest.integration.config.ts",
    "worker:reminders": "tsx src/reminders/worker.ts"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "express": "^5.0.0",
    "jose": "^6.2.12",
    "pg": "^8.23.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^26.6.1",
    "@types/pg": "^8.23.1",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.0.0",
    "tsx": "^4.23.13",
    "vitest": "^5.0.0"
  }
}
